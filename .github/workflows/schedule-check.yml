name: Schedule check

on:
  schedule:
    - cron: "*/5 * * * *"   # каждые 5 минут (UTC)
  workflow_dispatch: {}      # возможность ручного запуска

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print UTC time
        run: echo "It works! $(date -u)"
